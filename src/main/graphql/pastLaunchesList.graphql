query pastLaunchesList($limit: Int!) {
    launchesPast(limit: $limit) {
        id
        mission_id
        links {
            flickr_images
            mission_patch_small
        }
        rocket {
            rocket_name
        }
        launch_date_utc
    }
}